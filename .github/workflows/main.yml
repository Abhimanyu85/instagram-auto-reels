import os
from instagrapi import Client
from gnews import GNews
import requests
import random

IG_USERNAME = os.getenv("IG_USERNAME")
IG_PASSWORD = os.getenv("IG_PASSWORD")
GNEWS_KEY = os.getenv("GNEWS_KEY")

# 1. News lena
google_news = GNews(language='hi', country='IN', max_results=1)
news = google_news.get_news('trending')
title = news[0]['title']
url = news[0]['url']

# 2. Login
cl = Client()
cl.login(IG_USERNAME, IG_PASSWORD)

# 3. Reel upload - yahan tumhara video/image ka path dena hai
# Abhi demo ke liye text reel
cl.clip_upload(
    path="reel.mp4", # is jagah apni video ka naam
    caption=f"{title}\n\n#news #trending #vibehub",
    extra_data={"custom_accessibility_caption": title}
)

print("Reel upload ho gayi!")
